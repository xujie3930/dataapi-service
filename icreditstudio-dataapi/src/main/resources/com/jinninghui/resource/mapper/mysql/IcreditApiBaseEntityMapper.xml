<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinninghui.datasphere.icreditstudio.dataapi.mapper.IcreditApiBaseMapper">
    <select id="isExistByName" resultType="java.lang.Boolean">
        select 1
        from icredit_api_base
        where del_flag = 0
        <if test="name != null and name != '' ">
            AND name = #{name}
        </if>
        limit 1
    </select>

    <select id="isExistByPath" resultType="java.lang.Boolean">
        select 1
        from icredit_api_base
        where del_flag = 0
        <if test="path != null and path != '' ">
            AND path = #{path}
        </if>
        limit 1
    </select>
</mapper>
